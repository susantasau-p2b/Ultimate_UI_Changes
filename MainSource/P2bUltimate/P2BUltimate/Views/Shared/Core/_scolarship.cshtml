@{
    Layout = null;
}
@model P2b.Global.Scolarship
<style>
    .sco_popup-content-textbox {
        margin-bottom: 1px;
        min-width: 140px;
    }

    .sco_popup-content-label {
        display: block;
        margin: 1px;
        float: left;
        width: 95px;
    }
</style>

<div id="Create_Dialog" title="Create Skill">

    @using (Html.BeginForm(null, null, FormMethod.Post, new { Id = "frmScolarship", name = "frmScolarshipCreate" }))
    {
        <input id="Autho_Action" type="hidden" name="Autho_Action" />
        <input id="Autho_Allow" type="hidden" name="Autho_Allow" />
        <div class="sco_row1 content_margine">
            @Html.LabelFor(m => m.Name, "Name :", new { @class = "sco_popup-content-label must" })
            @Html.TextBoxFor(m => m.Name, new { @class = "sco_popup-content-textbox must" })
        </div>
        <div class="sco_row1 content_margine">
            @Html.LabelFor(m => m.Details, "Details :", new { @class = "sco_popup-content-label must" })
            @Html.TextBoxFor(m => m.Details, new { @class = "sco_popup-content-textbox must"})
        </div>
        <div class="sco_row1 content_margine">
            @Html.LabelFor(m => m.IssueDate, "Issue Date :", new { @class = "sco_popup-content-label must" })
            @Html.TextBoxFor(m => m.IssueDate, new { @class = "sco_popup-content-textbox date must", id = "IssueDate" })
        </div>
    }
</div>


<script>
    $(function () {
        $('.drop').selectmenu().selectmenu('menuWidget');
        $('.date').P2BDatePicker();
        $('#Name').ValidateTextBox({ maxlength: 50, name: true });
        $('#Details').ValidateTextBox({ maxlength: 50, name: true });
        $('#IssueDate').attr('readonly', 'readonly')
    });

    $(document).ready(function (e) {
       // $('#Details');
        Caption = 'Scholarship Details';


        $('<div></div>').P2BPartialCreateModalDialog();
        $('#Autho').makeDisable('#Create,#Edit,#Delete');
        $('#Autho').onClickGrid('#JqGrid', '@Url.Action("P2BGrid")', '@Url.Action("P2BGrid")');
    });
</script>